It's a superset of javascript that adds dynamic typing.,It's a sequence of nodes. each node points towards the next node forming a chain-like structure.,A stack is a data structure that its elements can only be accessed. starting from the topmost to the bottom element.,OOPs stands for Object-Oriented Programming System. a paradigm that provides concepts such as objects. classes. and inheritance.